#pragma once
#include <Core/Math/Constants.h>

namespace Pu
{
	/* Defines the types of buttons supported by the mouse. */
	enum class MouseButtons
		: uint16
	{
		Left,
		Right,
		Middle,
		Extra1,
		Extra2
	};
}